window.addEventListener("DOMContentLoaded", function(){
  let count = null,
  // lest = null,
  // max = 25,
  input_area = document.getElementById("stylist_catchphrase"),
  output_count = document.getElementById("count");
  // output_lest = document.getElementById("textLest"),
  // attention = document.getElementById("textAttention");

  input_area.onkeyup = function(){
      let length = input_area.value.length;
      count = length;
      // lest =  max - length;
      // output_lest.innerText = lest;
      output_count.innerText = count;
      // attention.style.display = ( length > max ) ? "block" : "none";
  }
}, false);
window.addEventListener("DOMContentLoaded", function(){
  let count = null,
  // lest = null,
  // max = 25,
  input_area = document.getElementById("stylist_self_introduction"),
  output_count = document.getElementById("count1");
  // output_lest = document.getElementById("textLest"),
  // attention = document.getElementById("textAttention");

  input_area.onkeyup = function(){
      const length = input_area.value.length;
      count = length;
      // lest =  max - length;
      // output_lest.innerText = lest;
      output_count.innerText = count;
      // attention.style.display = ( length > max ) ? "block" : "none";
  }
}, false);
window.addEventListener("DOMContentLoaded", function(){
  let count = null,
  // lest = null,
  // max = 25,
  input_area = document.getElementById("stylist_rank1"),
  output_count = document.getElementById("count2");
  // output_lest = document.getElementById("textLest"),
  // attention = document.getElementById("textAttention");

  input_area.onkeyup = function(){
      let length = input_area.value.length;
      count = length;
      // lest =  max - length;
      // output_lest.innerText = lest;
      output_count.innerText = count;
      // attention.style.display = ( length > max ) ? "block" : "none";
  }
}, false);
window.addEventListener("DOMContentLoaded", function(){
  let count = null,
  // lest = null,
  // max = 25,
  input_area = document.getElementById("stylist_technique"),
  output_count = document.getElementById("count3");
  // output_lest = document.getElementById("textLest"),
  // attention = document.getElementById("textAttention");

  input_area.onkeyup = function(){
      let length = input_area.value.length;
      count = length;
      // lest =  max - length;
      // output_lest.innerText = lest;
      output_count.innerText = count;
      // attention.style.display = ( length > max ) ? "block" : "none";
  }
}, false);
window.addEventListener("DOMContentLoaded", function(){
  let count = null,
  // lest = null,
  // max = 25,
  input_area = document.getElementById("stylist_hobby"),
  output_count = document.getElementById("count4");
  // output_lest = document.getElementById("textLest"),
  // attention = document.getElementById("textAttention");

  input_area.onkeyup = function(){
      let length = input_area.value.length;
      count = length;
      // lest =  max - length;
      // output_lest.innerText = lest;
      output_count.innerText = count;
      // attention.style.display = ( length > max ) ? "block" : "none";
  }
}, false);
window.addEventListener("DOMContentLoaded", function(){
  let count = null,
  // lest = null,
  // max = 25,
  input_area = document.getElementById("new_menu_field"),
  output_count = document.getElementById("count5");
  // output_lest = document.getElementById("textLest"),
  // attention = document.getElementById("textAttention");

  input_area.onkeyup = function(){
      let length = input_area.value.length;
      count = length;
      // lest =  max - length;
      // output_lest.innerText = lest;
      output_count.innerText = count;
      // attention.style.display = ( length > max ) ? "block" : "none";
  }
}, false);
window.addEventListener("DOMContentLoaded", function(){
  let count = null,
  // lest = null,
  // max = 25,
  input_area = document.getElementById("new_menu_text"),
  output_count = document.getElementById("count6");
  // output_lest = document.getElementById("textLest"),
  // attention = document.getElementById("textAttention");

  input_area.onkeyup = function(){
      let length = input_area.value.length;
      count = length;
      // lest =  max - length;
      // output_lest.innerText = lest;
      output_count.innerText = count;
      // attention.style.display = ( length > max ) ? "block" : "none";
  }
}, false);